# 未激活的项目自动化模板

此目录不会被 GitHub Actions 自动执行。

- `workflows/`：看板操作、README 看板、通知和 Skill 清理工作流的迁移模板。
- `config/`：不含真实账号、手机号或密钥的项目配置示例。

项目初始化后也不能直接批量复制并视为完成。模板已经包含统一 `active`/capability guard；安装前仍应按 `../SOP-BOOTSTRAP.md` 检查 Environment、Secrets、权限、成员分支和新项目配置，并在受审提交中逐文件启用。
